# Copy to backend/.env.railway-restore and fill from Railway MySQL service variables.
# Used only when running: npm run db:restore:railway
DB_HOST=reseau.proxy.rlwy.net
DB_PORT=59529
MASTER_DB_USER=root
MASTER_DB_PASSWORD=
DB_SSL=false
